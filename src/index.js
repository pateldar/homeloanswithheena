import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import HomeLoansWithHeena from './HomeLoansWithHeena';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <HomeLoansWithHeena />
  </React.StrictMode>
);
